-r requirements.txt

# Loads .env into os.environ so manage.py works without manually exporting vars.
python-dotenv==1.0.1

# Test and lint tooling (PLAN §2).
pytest==8.4.2
pytest-django==4.14.0
ruff==0.12.12

# Optional real LLM providers (AI_PROVIDER=anthropic|nvidia, see docs/decisions.md).
# Not needed to run or test the app: the mock provider is the default and
# each adapter's tests mock the SDK client, so no API key is ever required.
anthropic==0.125.0
openai==3.19.2

# Dev shell utilities.
ipython==9.17.1
django-extensions==4.1
